# Iconify

This tools converts 16x16px PNGs to Slime OS icons.

Place a file called `ICON_NAME.png` in this folder then run...

```
uv run iconify.py
```

And it will print out all icons in the folder
